You can connect different email server through Email component.
We use SMTP protocol to send emails, So, you can set the SMTP server address and port in the component configuration.
We use IMAP protocol to fetch emails, So, you can set the IMAP server address and port in the component configuration.
